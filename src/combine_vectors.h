#ifndef COMBINE_VECTORS_H
#define COMBINE_VECTORS_H

#include <vector>

namespace combineVectors
{

std::vector<int> combine_vectors(const std::vector<int>& vec_1, const std::vector<int>& vec_2);


} //namespace combineVectors

#endif //COMBINE_VECTORS_H